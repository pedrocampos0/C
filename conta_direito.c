int faz_conta_direito(int parcelas, char op)
{
    int i;
    int soma, sub, div, mul;
    scanf("%d", &parcelas);
    int C[parcelas];
    int sub[parcelas];
    if (op == '-')
    {
        for (i = 0; i < parcelas; i++)
        {
            scanf ("%d", &C[i]);
            sub = C[i];
        }
        
    }
    
    
    return 0;
}